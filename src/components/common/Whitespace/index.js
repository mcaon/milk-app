import React from 'react';
import { WhiteSpaceStyled } from './styles';

const Whitespace = ({ height, width }) => {
  return <WhiteSpaceStyled height={height} width={width} />;
};

export default Whitespace;
